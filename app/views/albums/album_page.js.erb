$('#feed').append('<%= j render @albums %>');
<% if @albums.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @albums %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
